_hotelsReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/{name}-review-H{srch}"
    defaults: { _controller: TTBundle:Reviews:hotelsReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "searching for cheap hotels, Want to know more about %s hotel tourist tube perfect site for best hotels", seokeywords: "", name: "", srch: 0  }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"

_ThingstodoReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/{name}-review-T{srch}"
    defaults: { _controller: TTBundle:Reviews:thingstodoReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "%s Best attractions to visit in %s are %s attractions, many other touristic places, tourist tube", seokeywords: "", name: "", srch: 0 }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"

_AirportReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/{name}-review-A{srch}"
    defaults: { _controller: TTBundle:Reviews:airportReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "searching for international airport %s, click on tourist tube for information about best airports in world", seokeywords: "", name: "", srch: 0 }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"

_review_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/review"
    defaults: { _controller: TTBundle:Reviews:review, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "review and rate | Tourist Tube", seodescription: "Find best vacation ideas and inspiration from Tourist tube, your source for the world's best hotels restaurant reviews.", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_ajax_add_review_lang:
    path:      "/{_locale}/ajax/add_review"
    defaults: { _controller: TTBundle:Reviews:addReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_ajax_remove_review_lang:
    path:      "/{_locale}/ajax/remove_review"
    defaults: { _controller: TTBundle:Reviews:removeReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_ajax_add_review_page_image_lang:
    path:      "/{_locale}/ajax/add_review_page_image"
    defaults: { _controller: TTBundle:Reviews:addReviewPageImage, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_ajax_remove_review_page_image_lang:
    path:      "/{_locale}/ajax/remove_review_page_image"
    defaults: { _controller: TTBundle:Reviews:removeReviewPageImage, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_ajax_add_rate_lang:
    path:      "/{_locale}/ajax/add_rate"
    defaults: { _controller: TTBundle:Reviews:addRate, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: ""}
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"

_old_hotel_review_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/thotel/id/{srch}"
    defaults: { _controller: TTBundle:Reviews:oldHotelReview, fromalbum: 1, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "", seodescription: "", seokeywords: "", srch: "" }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        srch: ".*"
        
_old_hotelsReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/hotel-review/id/{srch}/{name}"
    defaults: { _controller: TTBundle:Reviews:oldhotelsReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "searching for cheap hotels, Want to know more about %s hotel tourist tube perfect site for best hotels", seokeywords: "", name: "", srch: 0  }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"
        
_old_ThingstodoReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/things2do-review/id/{srch}/{name}"
    defaults: { _controller: TTBundle:Reviews:oldthingstodoReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "%s Best attractions to visit in %s are %s attractions, many other touristic places, tourist tube", seokeywords: "", name: "", srch: 0 }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"
        
_old_AirportReview_lang:
    host: "www{subdomain_suffix}.{domain}"
    path:      "/{_locale}/airport-review/id/{srch}/{name}"
    defaults: { _controller: TTBundle:Reviews:oldairportReview, _locale: "%locale%", domain: "%domain%", subdomain_suffix: "%subdomain_suffix%", seotitle: "%s %s review-tourist tube", seodescription: "searching for international airport %s, click on tourist tube for information about best airports in world", seokeywords: "", name: "", srch: 0 }
    requirements:
        _locale:  "%config_locales%"
        domain: "[^/]+"
        subdomain_suffix: ".*"
        name: ".*"
        srch: "\d*"